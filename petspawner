-- 💡 Full MoonCat Freezer - Disables Animations, Motors, and Movement

local player = game:GetService("Players").LocalPlayer
local screenGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
screenGui.Name = "FreezeLog"
screenGui.ResetOnSpawn = false

local logBox = Instance.new("TextLabel", screenGui)
logBox.Size = UDim2.new(0, 500, 0, 400)
logBox.Position = UDim2.new(0, 20, 0, 20)
logBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
logBox.TextColor3 = Color3.fromRGB(0, 255, 0)
logBox.Font = Enum.Font.Code
logBox.TextSize = 14
logBox.TextXAlignment = Enum.TextXAlignment.Left
logBox.TextYAlignment = Enum.TextYAlignment.Top
logBox.TextWrapped = true
logBox.Text = ""

local function log(msg)
	logBox.Text = logBox.Text .. msg .. "\n"
end

local function freezeMoonCatsProperly()
	local petsFolder = workspace:FindFirstChild("PetsPhysical")
	if not petsFolder then return log("❌ PetsPhysical not found") end

	local mover = petsFolder:FindFirstChild("PetMover")
	if not mover then return log("❌ PetMover not found") end

	local frozenCount = 0
	for _, cat in pairs(mover:GetChildren()) do
		if cat:IsA("Model") and cat.Name == "Cat" then
			frozenCount += 1
			log("🐾 Freezing MoonCat #" .. frozenCount)

			local frozenClone = cat:Clone()
			frozenClone.Name = "FrozenMoonCat_" .. tostring(frozenCount)

			-- Remove Motor6Ds to stop limb animation
			for _, d in pairs(frozenClone:GetDescendants()) do
				if d:IsA("Motor6D") then
					d:Destroy()
				elseif d:IsA("AnimationController") or d:IsA("Animator") then
					d:Destroy()
				elseif d:IsA("BasePart") then
					d.Anchored = true
					d.CanCollide = false
					d:SetNetworkOwner(nil)
				end
			end

			-- Set position to same location
			local root = cat:FindFirstChild("RootPart") or cat.PrimaryPart or cat:FindFirstChildWhichIsA("BasePart")
			if root then
				frozenClone:SetPrimaryPartCFrame(root.CFrame)
			end

			-- Freeze passive visually by cloning effect holders (if any)
			frozenClone.Parent = mover
		end
	end

	if frozenCount == 0 then
		log("⚠️ No MoonCats found to freeze.")
	else
		log("✅ Done freezing " .. frozenCount .. " MoonCats.")
	end
end

freezeMoonCatsProperly()
