for _,v in pairs(getgc(true)) do
    if typeof(v) == "function" and islclosure(v) and debug.getinfo(v).name == "FireServer" then
        hookfunction(v, function(...)
            print("📤 Remote Fired:", ...)
            return v(...)
        end)
    end
end
