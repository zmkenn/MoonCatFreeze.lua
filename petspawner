local args = {
    "givePet", -- This might be different depending on your logger!
    "Frog",    -- Pet name
    6,         -- Age (integer)
    4.29       -- Weight (decimal)
}

game:GetService("ReplicatedStorage").GameEvents.DataStream:FireServer(unpack(args))
