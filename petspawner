-- ✅ GUI Pet Spawner Script for Grow a Garden (Updated for visibility, dragging, and big button)
-- Use this in Delta after uploading to your GitHub:
-- loadstring(game:HttpGet("https://raw.githubusercontent.com/zmkenn/MoonCatFreeze.lua/main/PetSpawnerGUI.lua"))()

repeat wait() until game:IsLoaded() and game.Players.LocalPlayer and game.Players.LocalPlayer:FindFirstChild("PlayerGui")

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- GUI Setup
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "PetSpawnerUI"
screenGui.ResetOnSpawn = false
screenGui.IgnoreGuiInset = true
screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 350, 0, 240)
frame.Position = UDim2.new(0.5, -175, 0.5, -120)
frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Parent = screenGui

local uiCorner = Instance.new("UICorner", frame)
uiCorner.CornerRadius = UDim.new(0, 10)

local function createInput(name, posY)
    local label = Instance.new("TextLabel")
    label.Text = name .. ":"
    label.Position = UDim2.new(0, 10, 0, posY)
    label.Size = UDim2.new(0, 80, 0, 30)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.new(1, 1, 1)
    label.Font = Enum.Font.SourceSansBold
    label.TextSize = 20
    label.Parent = frame

    local input = Instance.new("TextBox")
    input.PlaceholderText = name
    input.Position = UDim2.new(0, 100, 0, posY)
    input.Size = UDim2.new(0, 230, 0, 30)
    input.Font = Enum.Font.SourceSans
    input.TextSize = 20
    input.TextColor3 = Color3.new(0, 0, 0)
    input.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    input.ClearTextOnFocus = false
    input.Parent = frame

    return input
end

local petInput = createInput("Pet Name", 10)
local ageInput = createInput("Age", 55)
local weightInput = createInput("Weight", 100)

local spawnBtn = Instance.new("TextButton")
spawnBtn.Position = UDim2.new(0.5, -100, 0, 150)
spawnBtn.Size = UDim2.new(0, 200, 0, 60)
spawnBtn.Text = "SPAWN PET"
spawnBtn.Font = Enum.Font.SourceSansBold
spawnBtn.TextSize = 26
spawnBtn.TextColor3 = Color3.new(1, 1, 1)
spawnBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
spawnBtn.Parent = frame

local corner = Instance.new("UICorner", spawnBtn)
corner.CornerRadius = UDim.new(0, 6)

spawnBtn.MouseButton1Click:Connect(function()
    local petName = petInput.Text
    local age = tonumber(ageInput.Text)
    local weight = tonumber(weightInput.Text)

    if not petName or petName == "" then return end

    local template = nil
    for _, obj in ipairs(ReplicatedStorage:GetDescendants()) do
        if (obj:IsA("Tool") or obj:IsA("Model")) and obj.Name == petName then
            template = obj
            break
        end
    end

    if not template then
        warn("Pet not found in ReplicatedStorage: " .. petName)
        return
    end

    local clone = template:Clone()

    if clone:IsA("Tool") then
        clone.Parent = LocalPlayer.Backpack
    else
        clone.Parent = workspace
        local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp and clone.PrimaryPart then
            clone:SetPrimaryPartCFrame(hrp.CFrame * CFrame.new(0, 5, 0))
        end
    end

    if clone:FindFirstChild("Age") then
        clone.Age.Value = age or 0
    end
    if clone:FindFirstChild("Weight") then
        clone.Weight.Value = weight or 0
    end

    print("✅ Spawned " .. petName .. " with Age: " .. tostring(age) .. ", Weight: " .. tostring(weight))
end)

print("✅ Pet Spawner GUI loaded")
